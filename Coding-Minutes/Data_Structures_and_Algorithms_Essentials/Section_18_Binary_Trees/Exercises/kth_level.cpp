/*
 * Given a binary tree with N nodes, print its Kth level.
 */
#include <iostream>
#include <vector>

struct Node {
    int key;
    Node *left, *right;
};

std::vector<int> printKthLevel(Node* root, int k) {
    std::vector<int> answer;
}

int main() {
}
