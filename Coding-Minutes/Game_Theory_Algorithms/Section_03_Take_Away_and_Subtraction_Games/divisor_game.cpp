/*
 * Divisor Game
 * Alive starts first
 * Initially there is anumber n on the chalkboard and on each
 * player's turn, that player makes a move consisting of:
 * - choosing any x where 0 < x < n and n % x == 0
 * - replace the number n on the chalkboard with n - x
 * If a player cannnot make a move, they losse.
 * Return true is Alive wins the game.
 */
#include <iostream>

bool divisorGame(int n) {
    return false;
}