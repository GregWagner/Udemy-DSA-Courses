#include <iostream>
#include <vector>

int solve(std::vector<int> nums) {
}

int main() {
    std::cout << std::boolalpha;

    std::vector<int> a { 1, 2, 3, 4, 5 };
    std::cout << (solve(a) == 33) << '\n';
}
